apiVersion: v1
kind: Service
metadata:
  name: {{ include "yolo-app.fullname" . }}
  labels:
    {{- include "yolo-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "yolo-app.selectorLabels" . | nindent 4 }}
