name: YOLO Cloud-Native CI Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  lint-and-test:
    name: Lint & Test Application
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v7

      - name: Setup Go Environment
        uses: actions/setup-go@v5
        with:
          go-version: '1.20'
          cache-dependency-path: app/backend/go.sum

      - name: Install Linting Tools
        run: |
          go install github.com/golangci/golangci-lint/cmd/golangci-lint@v1.54.2

      - name: Run Golang CI Lint
        run: |
          cd app/backend
          golangci-lint run --timeout=5m

      - name: Run Unit Tests
        run: |
          cd app/backend
          go test -v -race -coverprofile=coverage.out ./...

      - name: Upload Test Coverage
        uses: actions/upload-artifact@v4
        with:
          name: code-coverage-report
          path: app/backend/coverage.out
          if-no-files-found: ignore

      - name: Setup Helm
        uses: azure/setup-helm@v4
        with:
          version: 'v3.14.0'

      - name: Lint Helm Chart
        run: |
          helm lint deploy/helm/yolo-app

  build-and-scan:
    name: Build & Scan Docker Image
    needs: lint-and-test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v7

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Local Image
        uses: docker/build-push-action@v5
        with:
          context: ./app/backend
          file: ./app/backend/Dockerfile
          load: true
          tags: yolo-api:local
          build-args: |
            VERSION=1.0.0
            REVISION=${{ github.sha }}

      - name: Run Trivy Vulnerability Scanner
        uses: aquasecurity/trivy-action@0.20.0
        with:
          image-ref: 'yolo-api:local'
          format: 'table'
          exit-code: '1' # Fail build if vulnerabilities found
          ignore-unfixed: true
          vuln-type: 'os,library'
          severity: 'CRITICAL,HIGH'

      # Example step for pushing to registry (uncomment/configure in production)
      # - name: Log in to DockerHub
      #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      #   uses: docker/login-action@v2
      #   with:
      #     username: ${{ secrets.DOCKERHUB_USERNAME }}
      #     password: ${{ secrets.DOCKERHUB_TOKEN }}
      #
      # - name: Build and Push Production Image
      #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      #   uses: docker/build-push-action@v4
      #   with:
      #     context: ./app/backend
      #     file: ./app/backend/Dockerfile
      #     push: true
      #     tags: |
      #       melvinjosec/yolo-api:${{ github.sha }}
      #       melvinjosec/yolo-api:latest
